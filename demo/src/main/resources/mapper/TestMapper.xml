<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lxj.demo.dao.TestMapper">
    <insert id="insert" parameterType="cn.lxj.demo.domain.Order">
        insert into t_order
        (user_id, name)
        values
        (#{userId}, #{name})
    </insert>
</mapper>